<h1>All Posts</h1>
<% @posts.each do |post| %>
  <h2><%= link_to post.title, post %></h2>
  <p><%= post.content %></p>
<% end %>

<%= link_to 'Destroy', Post, method: :delete, data: { confirm: 'Are you sure?' } %>
